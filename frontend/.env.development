# Frontend Environment Variables - Development
# Note: In Vite, all env variables must be prefixed with VITE_

# Backend API URL (used in production)
# For development, the proxy in vite.config.js handles /api requests
VITE_API_URL=http://localhost:8001
